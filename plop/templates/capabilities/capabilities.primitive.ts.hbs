import { domain } from "@xndrjs/domain-zod";

export const {{pascalCapabilitiesName}}Capabilities = domain.capabilities
  .forPrimitive<{{primitiveScalar}}>()
  .methods(({ create }) => ({
    // TODO: add capabilities
  }));
