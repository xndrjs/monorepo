// Run from repo root: pnpm test:react
import { describe, expect, it } from "vitest";

describe("{{uiPackageName packageName}}", () => {
  it("fails", async () => {
    expect(false).toBe(true); // intentionally fails: remember to write real tests
  });
});
