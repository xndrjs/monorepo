/**
 * Use case "{{camelUseCaseName}}"
 *
 * Application flow:
 * // TODO (doc)
 * — Why the application flow needs this use case; which problem it solves.
 *
 * Design decisions:
 * // TODO (doc) 
 * — Non-trivial choices, trade-offs, or assumptions.
 *
 * High level behavior:
 * // TODO (doc) 
 * — What this use case orchestrates in domain terms (no line-level detail).
 *
 * Process placement:
 * // TODO (doc) 
 * — Where this use case fits in the broader process; what typically happens before/after.
 *
 * Wire it in the app composition root with `create{{pascalUseCaseName}}UseCase(deps)`.
 * Use cases should not import each other.
 */
export interface {{pascalUseCaseName}}UseCaseDeps {
  // Add required ports here.
}

export function create{{pascalUseCaseName}}UseCase(_deps: {{pascalUseCaseName}}UseCaseDeps) {
  return async function {{camelUseCaseName}}() {
    throw new Error("Not implemented: {{pascalUseCaseName}} use case");
  };
}
